# 系统双端体验深度优化与重构任务清单

## 第一阶段：PC 端 (WebGIS) 深度优化
- [x] 1. 剔除冗余代码状态 (`activeModule === 'layers'`)。
- [x] 2. 移除消耗性能的 `setInterval` 动画机制，改用 CSS 或控制合理的动画逻辑（或只移除卡顿代码）。
- [x] 3. 为“商机入驻”和“随手拍”表单增加 `isPickingCoordinate` 地图选点模式，取消手动填写经纬度的低效体验。

## 第二阶段：移动端 (小程序) 交互升维
- [x] 4. 在地图中心增加悬浮的“准心” UI (`+`)，明确随手拍选点中心。
- [x] 5. 为底部弹窗 (随手拍/招商) 添加从下方滑出的平滑过渡 CSS 动画效果。
- [x] 6. 改造“打卡领红包”逻辑，加入 `uni.getLocation` 距离计算功能，并提供一个模拟开关。

## 第三阶段：后端支持与测试
- [x] 7. 确保后端 `/cultural/checkin` 和表单提交在前端表现良好，使用 toast/modal 予以合理的反馈。
